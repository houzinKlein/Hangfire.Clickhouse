// The code namespace is `HangfireCH` (no longer nested under `Hangfire`), so root-namespace
// Hangfire types (JobStorage, GlobalConfiguration, …) need to be imported explicitly.
global using Hangfire;
